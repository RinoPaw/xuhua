# 叙华界面设计验收

## 验收范围

- 参考图：`.superdesign/references/xuhua-high-fidelity-direction.png`
- 桌面实现：`.superdesign/qa/implementation-desktop-final.png`（1672 × 941）
- 同视口对照：`.superdesign/qa/comparison-final.png`
- 平板实现：`.superdesign/qa/implementation-tablet-final.png`（1024 × 768）
- 手机实现：`.superdesign/qa/implementation-mobile-final.png`（390 × 844，真实设备视口）

## 已确认的设计取舍

项目卡片和引用列表不使用项目缩略图，也不保留纯色占位区。这是用户明确要求的取舍；信息密度改由标题、级别、类别、简介、校园呈现、适合场景和引证承担。数字人、木纹、宣纸、织锦、印章与顶部纹样继续使用真实素材。

## 迭代记录

1. Pass 1：建立木构、宣纸、织锦、金线与三栏骨架，并加入项目缩略图。
2. Pass 2：按要求删除项目图片；发现卡片留白过大（P1）。
3. Pass 3：将卡片重构为完整的文字档案单元，补齐场景与引证；清理重复 CSS。
4. Final：替换并放大数字人，删除全部假按钮，修正平板卡片密度、手机输入区、焦点与实时状态语义。

## 问题清单

| 级别 | 问题 | 结论 |
| --- | --- | --- |
| P0 | 无阻断问题 | 通过 |
| P1 | 纯色背景与临时占位造成完成度不足 | 已用真实木纹、宣纸、织锦、印章和纹样素材替换 |
| P1 | 项目图片删除后卡片失衡 | 已改为结构化文字档案；无空图片框或占位图 |
| P1 | 数字人比例偏小、主视觉不明确 | 已替换近景全身数字人并提高画面占比 |
| P1 | 861–1180px 四列卡片过窄 | 已在中宽切换两列并允许中心栏收缩 |
| P1 | 手机端继续提问需要长距离滚动 | 已固定对话视窗，让输入和朗读控制常驻底部 |
| P1 | 页面存在无行为的历史、收藏、指南和来源按钮 | 已删除，顶部只保留非交互资料库状态 |
| P1 | 流式回答缺少辅助技术状态 | 已加入 `aria-live`、`aria-busy` 和折叠状态语义 |
| P2 | 输入焦点不够明确 | 已恢复控件级焦点环并保留 `focus-within` |
| P2 | 系统滚动条破坏视觉统一 | 已加入窄金棕色滚动条样式 |
| P2 | 项目名称在桌面卡片被截断 | 已收紧标题排版，完整显示名称 |
| P2 | CSS 重复覆盖与旧图片选择器 | 已清除，单一响应式样式源 |

## 功能与可访问性检查

- 问题提交、停止回答、语音朗读、资料检索、筛选与面板折叠均为真实行为。
- 发送按钮可键盘操作；输入具有标签、必填与长度限制。
- 支持 `prefers-reduced-motion`，所有主要控件具有可见焦点。
- 手机默认折叠人物与引用，先展示对话；桌面保持数字人为左侧主视觉。
- 项目图片相关运行时代码、资产和占位逻辑均已删除。

## 结论

最终桌面、平板和手机视口均通过验收。与参考图的唯一主动差异是按用户要求取消项目缩略图；该差异已通过文字信息结构补偿，不视为缺陷。
